<%- layout("/layouts/boilerplate.ejs")%>
    <div class="row mt-3">
        <div class="col-8 offset-3"> 
        <h3> Listing Details</h3>
        </div>

        <div class="card  col-6 offset-3 show-card">
            <img src="<%= list.image.url %>" class="card-img-top show-img" alt="listing_image">
            <div class="card-body">
                <p class="card-text">
                   <b> <%= list.title %> </b> <br>
                   <%= list.description %> <br>
                   $<%= list.price.toLocaleString("en-US") %> <br>
                   <%= list.location %><br>
                   <%= list.country %>
                </p>
            </div>
        </div>
     </div>

    <div class="btns">
          <a href="/listings/<%= list._id %>/edit" class="btn btn-dark col-1 offset-3 edit-btn">Edit</a>
         
            <form method="POST" action="/listings/<%= list.id %>?_method=DELETE " class="delete-form">
            <button  class="btn btn-dark  offset-5">Delete</button>
        </form>
       </div>
        
       
        <div id="confirmModal" class="modal">
                <div class="modal-content">
                    <p>Do you want to delete this Listing?</p>
                    <div class="modal-buttons">
                        <button id="confirmYes">Yes</button>
                        <button id="confirmNo">No</button>
                    </div>
                </div>
        </div>
